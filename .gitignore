node_modules/
dist/
data/
*.db
*.db-journal
*.db-shm
*.db-wal
.env
.DS_Store

# CSVs exportados localmente para importar — no subir
*.csv

# Tests que usan datos financieros REALES del usuario (su Excel del banco).
# El repo es publico: estos no se versionan nunca.
tools/local/
